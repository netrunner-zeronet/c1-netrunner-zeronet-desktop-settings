[Unit]
Description=Decentralized websites using Bitcoin crypto and the BitTorrent network

[Service]
User=zeronet
Group=zeronet
WorkingDirectory=/opt/zeronet
ExecStart=/usr/bin/env python2 zeronet.py --tor always --ui_ip "*"
Restart=on-failure

[Install]
WantedBy=default.target 
